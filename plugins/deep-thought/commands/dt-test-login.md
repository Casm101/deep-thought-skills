---
description: Authenticate a staging test player and hand back a Playwright storageState, so a browser context starts logged in.
---
Read `${CLAUDE_PLUGIN_ROOT}/skills/dt-test-login/SKILL.md` and execute it exactly as written, treating
the following as the user's input/arguments: $ARGUMENTS

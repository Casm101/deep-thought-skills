---
description: Turn a Claude artifact into one self-contained HTML file that opens anywhere with no network.
---
Read `${CLAUDE_PLUGIN_ROOT}/skills/dt-artifact-export/SKILL.md` and execute it exactly as written, treating
the following as the user's input/arguments: $ARGUMENTS

---
description: Bring a branch up to date with the origin's default branch, then commit and push the merge.
---
Read `${CLAUDE_PLUGIN_ROOT}/skills/dt-branch-update/SKILL.md` and execute it exactly as written, treating
the following as the user's input/arguments: $ARGUMENTS
